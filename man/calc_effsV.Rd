% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_effsV.R
\name{calc_effsV}
\alias{calc_effsV}
\title{Calculate Health Outcomes (QALYs)}
\usage{
calc_effsV(
  v_occupied_state,
  v_states_utilities,
  m_indi_features,
  v_util_coeffs,
  v_util_t_decs,
  v_time_in_state,
  cycle_length = 1
)
}
\arguments{
\item{v_occupied_state}{Character vector of current state for each
individual.}

\item{v_states_utilities}{Named numeric vector of base utilities for each
state.}

\item{m_indi_features}{Matrix or data.frame of individual covariates.}

\item{v_util_coeffs}{Numeric vector of regression coefficients for the
individual covariates used to calculate utility decrements.}

\item{v_util_t_decs}{Named numeric vector of time‑dependent utility
decrements for states S1 and S2. The names must correspond to state names.}

\item{v_time_in_state}{Numeric vector indicating the time spent in the
current state.}

\item{cycle_length}{Length of one cycle measured in years. Defaults to 1.}
}
\value{
A numeric vector of QALYs for each individual over the cycle.
}
\description{
Computes the quality adjusted life years accrued in a single cycle for each
individual. Utilities for states S1 and S2 are adjusted by individual
covariate coefficients and by the time spent in the state.
}
\examples{
v_occupied_state <- c("H", "S1", "S2", "D")
v_states_utilities <- c(H = 1, S1 = 0.75, S2 = 0.5, D = 0)
m_indi_features <- cbind(age = c(50, 60, 70, 80), sex = c(0, 1, 0, 1))
v_util_coeffs <- c(age = -0.0018, sex = -0.015)
v_util_t_decs <- c(S1 = -0.0015, S2 = -0.002)
v_time_in_state <- c(1, 2, 3, 4)

calc_effsV(
  v_occupied_state = v_occupied_state,
  v_states_utilities = v_states_utilities,
  m_indi_features = m_indi_features,
  v_util_coeffs = v_util_coeffs,
  v_util_t_decs = v_util_t_decs,
  v_time_in_state = v_time_in_state
)

}
